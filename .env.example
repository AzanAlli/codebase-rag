# Copy this file to .env and fill in real values.
# .env is already in .gitignore — never commit real credentials.

# Supabase Postgres connection (use the Session Pooler URI, not Direct)
DATABASE_URL=postgresql://postgres.xxxxxxxx:PASSWORD@aws-1-REGION.pooler.supabase.com:5432/postgres

# GCP / Vertex AI (Agent Platform)
GCP_PROJECT_ID=colab-bq-access-472521
GCP_REGION=us-central1
